---
title: "{{TOPIC_NAME}} Log"
type: log
tags: [log]
created: {{SETUP_DATE}}
updated: {{SETUP_DATE}}
---

> 近期活动速览（滚动窗口）——完整操作历史靠 git（`git log -p -- wiki/log.md`）。
>
> **追加纪律**：仅三种 wiki 痕迹入列（ingest 落盘 / query 归档 / lint 执行），触发判定
> 见 [`../AGENTS.md`](../AGENTS.md)「写入纪律」「写后必同步」条。正路走
> `llmw wiki write log`（格式 + 滚动窗口截断自动保证），带外手改按下列格式并手工截断。
>
> **格式**：`## [YYYY-MM-DD HH:MM] <op> | <title>`（op ∈ {{LOG_OPS}}；日期也接受 `YYYY-MM-DD`）。`ingest` 可追加第三段
> `| <raw 相对路径>`（wiki 根相对、`raw/` 起头；`write log --raw` 自动落）。
> `setup` 首条由 llmw CLI 初始化时写入；格式不符
> 会被 `llmw wiki lint` 报告。标题简洁、不超过一行；URL / 详细摘要写在对应页面里。
>
> **滚动窗口**：条目数 > {{LOG_RETENTION_LIMIT}} 时删最旧的保最近 {{LOG_RETENTION_LIMIT}} 条（本文件 frontmatter 不动）。

## [{{SETUP_DATE}}] setup | Initial scaffold by llmw CLI
